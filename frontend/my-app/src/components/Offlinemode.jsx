import React from 'react';

const OfflineMode = () => {
  return (
    <div className="offline-mode">
      <h2>Offline Mode Active</h2>
      <p>Minimal data is being logged locally.</p>
    </div>
  );
};

export default OfflineMode;